version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: progress-tracker-frontend

    ports:
      - "5173:80"

    restart: unless-stopped